# Leave unset in production so Cloud Run sibling auto-discovery can target the
# matching API service for each deployed environment. Only set this when the UI
# must point at a non-sibling API.
VITE_FIREBASE_DATABASE_ID=career-concierge
